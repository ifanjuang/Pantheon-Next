"""Pantheon Next — MCP policy server (bounded module).

Read-only / validation / candidate-preparation surface centered on the
capability passport. It serves doctrine, validates passports and returns
policy decisions as data. It executes nothing, approves nothing, writes
nothing and promotes no memory. See CLAUDE.md and
docs/governance/PANTHEON_MCP_POLICY_SERVER_DEVELOPMENT.md.
"""

__version__ = "0.1.0"
