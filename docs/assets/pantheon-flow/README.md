# Pantheon flow assets

Status: support visual assets.

## Current note — 2026-06-15

The standalone D3 asset `entries-outputs-memory-d3.html` remains useful, but the landing integration of `flow2a` introduced a mobile text-overlap regression after PR #136.

Issue #131 has been reopened for a focused hotfix of the mobile rendering in `docs/index.html`.

Boundary: visual documentation only; no runtime, approval engine, memory engine, connector, scheduler, queue or external action.
