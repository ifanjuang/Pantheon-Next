# Model Routing Policy

Status: obsolete — superseded; resolved per `STUB_RESOLUTION_PLAN.md`.

Provider routing is a capability Pantheon must not own; the rule now lives once in `BOUNDARY_STANDARD.md`. Routing itself belongs to the execution runtime (Hermes).

This file remains only so historical references keep resolving. It must not be used as authority.
