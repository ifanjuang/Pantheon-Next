# Memory Event Schema

Status: obsolete — superseded; resolved per `STUB_RESOLUTION_PLAN.md`.

Superseded by the E6 schema baseline: `schemas/register_candidate.schema.yaml` and `schemas/answer_status.schema.yaml` are the validated contracts for Registre material and answer status.

This file remains only so historical references keep resolving. It must not be used as authority.
