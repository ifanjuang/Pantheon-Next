# AI log — Hermes Agent 0.21.2 update

Date: 2026-09-12
Status: runtime update applied and verified.

## Change

- updated the reviewed Ubuntu deployment pin from
  `nousresearch/hermes-agent:v2026.9.7` to
  `nousresearch/hermes-agent:v2026.9.11`;
- aligned the Workspace Cockpit derivative image default with the same tag;
- rebuilt and recreated the read-only Workspace Cockpit image on that base,
  retaining its ignored host-local `192.168.50.135:8189` bind configuration;
- pulled and deployed the official amd64 image with local image ID and repo
  digest `sha256:9469b3e78b9545b6d576eb8887a95352e9a0ea83730eaf31431cf862ca1010e1`;
- Hermes migrated its configuration schema from 41 to 42 on startup.

## Recovery point

Hermes was stopped before copying `state.db` and its WAL/SHM/lock sidecars.
The cold database copy and the pre-update Compose/version files are retained at:

```text
/srv/pantheon/backups/update-20260912T195401Z
```

The v0.21.2 non-destructive `sessions recover --inspect-only` check reported
the preserved source bundle unchanged, recoverable, and free of errors or
warnings (65 sessions and 2,718 messages readable).

The persistent `/srv/pantheon/hermes` bind mount was retained. No session,
profile, SOUL file or Pantheon activity-projection skill was replaced by the
container recreation.

## Verification

- `hermes --version`: `Hermes Agent v0.21.2 (2026.9.11)`, upstream
  `939e45c9`;
- `hermes doctor`: configuration v42, `state.db` in WAL mode, 65 sessions,
  2,718 messages, FTS tables present, main Hermes and dashboard supervisors up;
- API health inside the host-networked service returned version `0.21.2`;
- the LAN dashboard status at `192.168.50.135:9119` returned version
  `0.21.2`, gateway running and API platform connected;
- the Workspace Cockpit health endpoint at `192.168.50.135:8189` returned
  `read_only: true`, with the container healthy;
- `pantheon-governed` remained running on `qwen3.5:27b`;
- the installed activity-projection skill retained SHA-256
  `1a54f4f5d07ca26d457fb9420e6db4d5c51b02b8b6eb0816b8cfea085d24cb23`.

## Boundary and residual advisories

This updates the Hermes runtime and its deployment candidate pin. It does not
change Pantheon authority, activation state, Role semantics or Evidence rules.

`hermes doctor` still reports five non-blocking, pre-existing setup advisories:
the container-local CLI symlink, npm audit findings in optional/web workspaces,
and unconfigured optional providers. No automatic `doctor --fix` or npm audit
mutation was applied as part of this scoped update.
