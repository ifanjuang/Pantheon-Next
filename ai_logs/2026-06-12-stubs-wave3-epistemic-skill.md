# AI log — Stub resolution wave 3: epistemic disposition + fresh SKILL_LIFECYCLE

Date: 2026-06-12.

## Intent

Execute the third disposition class of `STUB_RESOLUTION_PLAN.md`
(maintainer-approved), with the two refinements the maintainer accepted:
read the legacy source before deciding EPISTEMIC_CONTROL, and write
SKILL_LIFECYCLE fresh by distillation rather than migrating.

## EPISTEMIC_CONTROL decision (after reading legacy)

Read `Pantheon-OS-main/docs/governance/EPISTEMIC_CONTROL.md` (+ PROPAGATION)
from `legacy/Pantheon-OS-main.zip`. The substance — Claim Register, claim
statuses, declared confidence — is owned by current doctrine: the
`claim_record` shape of ANSWER_VERIFICATION_GATE, the EVIDENCE_PACK claim
ledger, and the E0-E4 axis. The legacy text also carries retired vocabulary
(`canonized`, low/medium/high confidence). Disposition: **obsolete with
pointer**, recording the one un-absorbed detail (the `claim_type`
taxonomy) as a candidate enrichment of the Answer Verification Gate.
Both stubs became redirect notes.

## SKILL_LIFECYCLE (fresh, candidate)

New `SKILL_LIFECYCLE.md` written by distillation, composing existing
doctrine only (manifest, passport, admission guard, preflight, Task
Contract): eight lifecycle states, one gate per transition, per-task
authorization, non-collapses, boundary by reference to
`BOUNDARY_STANDARD.md`. Probation/quarantine from the former stub map to
admitted-with-constraints and suspended.

## Index exception

The Lot 1 coverage check requires candidate docs to be indexed; one
AUTHORITY_INDEX row was added for SKILL_LIFECYCLE (minimal exception to
the indexing rule, recorded here).

## Boundary

Documentation only; nothing deleted; no protected path; no lifecycle
automation implemented.
