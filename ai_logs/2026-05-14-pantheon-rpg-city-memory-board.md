# Pantheon RPG city memory board

Date: 2026-05-14

## Change

Added documentation for the accepted planche 2 visual direction:

- `docs/assets/pantheon-rpg/prompts/prompt-city-memory-v01.md`;
- `docs/assets/pantheon-rpg/exports/pantheon-rpg-city-memory-v01.provenance.md`.

Updated:

- `docs/assets/pantheon-rpg/STATUS.md`.

## Reason

The visual work on the `Cité Mémoire Gouvernée` board reached an accepted direction.

The design is now captured as a reusable prompt and provenance note before moving to the next board.

## Accepted visual semantics

The board captures:

- Pantheon = system memory;
- District A / Case A = project memory;
- District B / Project B = project memory;
- District C / File C = project memory;
- pastures = knowledge;
- fields = templates;
- archives = archived projects;
- quays and docks = internet source intake;
- foundations = manifests;
- pigeonry = transmission;
- workshops = skill forging;
- domain selection = choose métier;
- deliverables factory = exports and renders.

## Governance boundary

This change is documentation and visual prompt work only.

It does not introduce:

- execution runtime;
- agent runtime;
- tool runtime;
- provider router;
- scheduler;
- workflow engine;
- memory auto-promotion;
- repository README integration;
- binary asset pipeline.

The image remains a communication asset and does not claim implementation.

## Limitation

The final PNG binary was generated during the working session but was not committed as a repository binary asset by this change.

The provenance file records the intended future path:

```text
docs/assets/pantheon-rpg/exports/pantheon-rpg-city-memory-v01.png
```

## Follow-up

Move to the next board in the Pantheon RPG visual system.

Recommended next board: `Monde des Possibles`, because it gives context to the desert route and floating domain selection already introduced in planche 2.
