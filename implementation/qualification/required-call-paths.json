{
  "schema_id": "pantheon.required_call_paths",
  "revision": 1,
  "purpose": "Pin the call-reachability state of the paths that are supposed to carry consequential effects, so a path that stops being taken fails a check instead of disappearing from view.",
  "how_to_read": [
    "expected_state is compared against the symbol layer of implementation/tools/audit_module_usage.py.",
    "Divergence fails in both directions: a path that was supposed to be taken and is not, and a path declared dead that has come alive without anyone updating this file.",
    "An entry expecting anything other than entry_reachable must name what blocks it, so a known dead path stays declared rather than becoming invisible again.",
    "entry_reachable is a weak claim. Call edges resolve by bare symbol name, so it means a chain of names reaches the symbol, not that a deployed run takes the path, and not that the call supplies real arguments. The semantic complement is the assembly and inventory tests under implementation/tests/."
  ],
  "paths": [
    {
      "id": "cockpit-app-factory",
      "symbol": "mvp_vertical.cockpit_shell:create_cockpit_app",
      "expected_state": "entry_reachable",
      "why": "The Cockpit application factory is the assembly point for every consequential API surface. If nothing reaches it, no surface is assembled at all."
    },
    {
      "id": "policy-chokepoint",
      "symbol": "mvp_vertical.policy_gate:enforce_consequential",
      "expected_state": "entry_reachable",
      "why": "The governance check itself. Reaching it is the floor, not the goal: the symbol layer cannot see that a caller passes no policy client, which is why the inventory and assembly tests exist alongside this entry."
    },
    {
      "id": "policy-decision-client",
      "symbol": "mvp_vertical.policy_gate:HttpPolicyClient",
      "expected_state": "test_called_only",
      "blocked_by": "No runtime code constructs the PDP client. Every consequential path that reaches enforce_consequential therefore reaches it without a decision point, and the gate resolves locally.",
      "why": "This is the dead path the module-level inventory could not see: the client is imported, typed, tested, and never built outside tests. Pinning it here means the day it becomes reachable, this file has to be updated deliberately rather than the change passing unremarked."
    },
    {
      "id": "governed-effect-wrapper",
      "symbol": "mvp_vertical.policy_gate:governed_effect",
      "expected_state": "test_called_only",
      "blocked_by": "No runtime caller wraps an effect in the gate; the wrapper exists and is exercised only by its own tests.",
      "why": "A gate that is implemented and never invoked reads as a gate that is in place. It is not the same thing, and this entry keeps the difference on the record."
    },
    {
      "id": "governed-capability-execution",
      "symbol": "mvp_vertical.capability_manager:governed_execute",
      "expected_state": "test_called_only",
      "blocked_by": "No runtime caller routes a capability execution through the governed path.",
      "why": "Capability execution is consequential by definition. Declaring the current state keeps 'the governed path exists' from being read as 'the governed path is used'."
    },
    {
      "id": "knowledge-update-apply",
      "symbol": "mvp_vertical.knowledge_update:apply_knowledge_update",
      "expected_state": "entry_reachable",
      "why": "The first owner-specific Knowledge mutation, and one of the two mutation entry points carrying the strongest local guard chain. Its route must stay connected to the application."
    },
    {
      "id": "handoff-admission",
      "symbol": "mvp_vertical.hermes_execution:admit_handoff",
      "expected_state": "entry_reachable",
      "why": "Admission of an external execution handoff. An admission path that is no longer reachable would silently stop being an admission path."
    },
    {
      "id": "authorized-write-command",
      "symbol": "mvp_vertical.apu_write_preparation:apply_authorized_write_command",
      "expected_state": "entry_reachable",
      "why": "The other mutation entry point with a real local guard chain."
    }
  ]
}
