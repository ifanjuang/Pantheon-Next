name: Revit local adapter convergence

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]
    paths:
      - .github/workflows/revit-local-adapter-convergence.yml

permissions:
  contents: write

jobs:
  converge:
    if: github.head_ref == 'agent/revit-local-adapter-convergence'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Consolidate the Revit adapter boundary
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          if [[ -f docs/governance/REVIT_LOCAL_ADAPTER.md ]] && grep -q "Canonical boundary marker: revit-local-adapter" docs/governance/REVIT_LOCAL_ADAPTER.md; then
            echo "Convergence already applied."
            exit 0
          fi

          python - <<'PY'
          from pathlib import Path
          import re

          root = Path('.')

          renames = {
              'docs/governance/PANTHEON_REVIT_GATE.md':
                  'docs/governance/REVIT_CAPABILITY_EXPLORATION.md',
              'docs/governance/PANTHEON_REVIT_LOCAL_SANDBOX_EXCEPTION.md':
                  'docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_POLICY.md',
              'docs/governance/PANTHEON_REVIT_FIRST_SANDBOX_ACTION_CONTRACT.md':
                  'docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_ACTION_EXAMPLE.md',
              'docs/governance/PANTHEON_REVIT_GATE_2027_PROTOTYPE_PLAN.md':
                  'docs/governance/REVIT_LOCAL_ADAPTER_PROTOTYPE_PLAN.md',
              'docs/governance/PANTHEON_REVIT_GATE_DEVELOPER_DOSSIER.md':
                  'docs/governance/REVIT_LOCAL_ADAPTER_DEVELOPER_DOSSIER.md',
          }

          for old, new in renames.items():
              old_path = root / old
              new_path = root / new
              if not old_path.is_file():
                  raise SystemExit(f'missing expected source: {old}')
              if new_path.exists():
                  raise SystemExit(f'target already exists: {new}')
              old_path.rename(new_path)

          text_extensions = {
              '.md', '.json', '.yaml', '.yml', '.toml', '.py', '.js', '.css',
              '.html', '.txt', '.sh'
          }
          for path in root.rglob('*'):
              if not path.is_file() or path.suffix not in text_extensions:
                  continue
              if '.git' in path.parts or 'ai_logs' in path.parts:
                  continue
              try:
                  text = path.read_text(encoding='utf-8')
              except UnicodeDecodeError:
                  continue
              updated = text
              for old, new in renames.items():
                  updated = updated.replace(old, new)
                  updated = updated.replace(Path(old).name, Path(new).name)
              if updated != text:
                  path.write_text(updated, encoding='utf-8')

          canonical = '''# Revit Local Adapter Boundary

Status: candidate support doctrine — canonical local-adapter boundary — documented non-implemented.
Boundary profile: external_runtime_adapter.
Canonical boundary marker: revit-local-adapter.

This document is the single authority for the placement of a future local Revit adapter in the Pantheon ecosystem. It consolidates the useful boundary material already present in the repository; it does not create a new semantic layer or claim that an add-in is installed, healthy, activated or authorized.

```text
Pantheon Next governs contracts, scope, status, Evidence posture and human gates.
pantheon-mvp may implement candidate APIs, projections and adapter seams.
Hermes may orchestrate an admitted task as an external runtime.
The Revit add-in alone executes Revit API calls inside Revit context.
Cockpit or OpenWebUI exposes preflight, warnings, proposals and results.
The human decides consequential mutations.
```

```text
installed != approved
healthy != safe
activated != task_authorized
preflight_passed != effect_authorized
transaction_success != accepted_result
runtime_success != Evidence
UI status != authorization
```

## Purpose

The adapter exists to connect a live Revit document to governed Pantheon work without moving Revit execution into Pantheon. It may observe exact model context, prepare bounded candidates and, after explicit authorization, execute a named local transaction through an external add-in.

This repository currently contains documentation, examples and a non-compiling reference skeleton only. It contains no active Revit add-in, installer, updater, relay, scheduler, queue, provider router, plugin manager, approval engine or autonomous transaction runner.

## Canonical responsibility split

### Pantheon Next

Pantheon Next owns:

```text
Capability Slot and binding boundaries
Task Contract and requested-effect vocabulary
scope and Context Pack requirements
approval ceiling and human-decision requirements
Claim, ChangeCandidate and Evidence posture
status semantics and non-equivalences
adapter adoption and revocation doctrine
```

Pantheon Next does not call the Revit API, open a transaction, save, synchronize or mutate an RVT file.

### pantheon-mvp

A future implementation candidate may own:

```text
bounded adapter API surfaces
Cockpit projections
preflight and result-candidate persistence
external-runtime handoff seams
technical observations and receipts
```

An implementation does not become adopted merely because it exists or passes CI.

### Hermes or another admitted runtime

An external runtime may:

```text
consume one admitted Task Contract and Context Pack
prepare a method or action candidate
request a bounded adapter operation
continue independent work while one action is blocked
return a Result Candidate and technical trace
```

It may not infer a broader scope, manufacture human approval, bypass the add-in, select a hidden provider on Pantheon's behalf or promote its result to Evidence.

### Revit add-in

The add-in owns:

```text
Revit API compatibility
active-document and active-view observation
selection and element resolution
freshness checks
Revit-thread execution
named Transaction and TransactionGroup discipline
failure handling and rollback observation
changed-element journaling
local stop and disable controls
```

The add-in is the only component allowed to execute Revit API mutations. Its production code should live in a dedicated implementation repository. The `revit-plugin/` directory in this repository is a non-executable reference skeleton and is not a code-hosting precedent.

### Cockpit or OpenWebUI

The user surface may display:

```text
binding and installation observations
Task Contract and exact scope
Context Pack summary
preflight observations and unresolved blockers
method and ChangeCandidate previews
affected elements
human decision controls
technical result and rollback posture
Evidence candidates and provenance references
```

The projection does not own business truth or authorization.

### Human

A human decision remains required for adapter adoption, binding activation and every consequential mutation. Acceptance of a technical result, application of a ChangeCandidate, Evidence admission, saving, synchronization, publication and transmission remain separate decisions.

## Reuse of existing Pantheon concepts

The Revit boundary reuses the current model rather than defining a parallel workflow.

| Need | Existing owner |
|---|---|
| declare the abstract function | Capability Slot and binding records |
| bound one request | Task Contract |
| carry exact project/model/view/selection context | Context Pack and source references |
| represent observed project understanding | Project Understanding adapter contract and Result Candidate |
| propose a consequential project/model change | ChangeCandidate |
| record technical execution | Trace / action report candidate / technical receipt |
| qualify supporting material | Evidence Pack Candidate, followed by separate admission |
| report an unavailable prerequisite | Capability Gap or typed blocker observation |
| expose work and decisions | Work Issue and Cockpit projections |

Labels such as `Model Observation Candidate`, `Preflight Report Candidate` or `Action Log Candidate` are presentation labels inside these existing envelopes. They are not new canonical entities.

## Capability and binding posture

```yaml
capability_id: revit_local_adapter
abstract_function: inspect and modify a bounded Revit document through a local add-in
binding_state: candidate
installation_state: not_observed
health_state: not_observed
activation_state: not_activated
task_authorization_state: not_authorized
executed_by: external Revit add-in
orchestrated_by: admitted external runtime when selected
governed_by: Pantheon Next
approved_by: human for consequential effects
```

The product version, .NET target, packaging method and Revit API compatibility belong to the binding observation. They must not be encoded in the architecture identity.

## Effect classification

The adapter may use the following local effect classes, provided they map to the Task Contract requested effect and approval ceiling:

| Adapter-local class | Meaning | Default posture |
|---|---|---|
| `read_only` | observe document, view, selection, elements, warnings or parameters | allowed only inside admitted scope |
| `candidate_only` | highlight, preview, analyze, prepare method or ChangeCandidate | no Revit mutation |
| `write_light` | create a bounded review artifact or explicitly allowed review parameter | human authorization and named transaction required |
| `write_model` | create or modify architectural model elements | blocked until a dedicated binding slice proves scope, preflight, rollback and review |
| `external_effect` | save, sync, publish, transmit, install, execute arbitrary code or alter shared state | refused unless a later explicit contract and human decision authorize the exact effect |

The former numbered warning levels are retained only as historical planning vocabulary. Their useful meaning converges as follows:

```text
historical observation level       -> read_only
historical proposal level          -> candidate_only
historical light-write level       -> write_light
historical model-write level       -> write_model
historical destructive/code levels -> external_effect or refusal
```

No numbered warning level is a Pantheon approval state.

## Admission and execution sequence

```text
1. A binding is selected and separately observed.
2. A human or governed surface prepares a Task Contract and exact Context Pack.
3. Pantheon records admission for the requested effect; no dispatch is inferred.
4. The external runtime prepares a method or ChangeCandidate when required.
5. The add-in performs a fresh preflight against the open Revit document.
6. A human authorizes the exact consequential effect when required.
7. The add-in executes one named local transaction or refuses.
8. The add-in returns changed-element observations, failures and rollback posture.
9. Hermes returns a Result Candidate and Trace.
10. Result acceptance, ChangeCandidate application and Evidence admission remain separate.
```

A stale document, view, selection, element set, phase, design option, workset or host invalidates the preflight. The correct result is a visible conflict or Capability Gap, not an improvised mutation.

## Preflight minimum

Before a write, the add-in should return at least:

```text
document identity and Revit binding version
active view and view type
exact target element references
selection or work-area source
phase and design-option context
worksharing, workset and ownership observations
linked-model involvement
pinned, grouped, constrained and hosted-element observations
family and type availability
requested effect and approval reference
transaction-name candidate
expected created/modified/deleted categories
rollback or manual-reversal posture
freshness token or equivalent document-state observation
forbidden-effect findings
```

A passed preflight is a technical observation. It does not authorize the effect.

## Transaction discipline

Every committed mutation must execute inside Revit context with a deterministic name:

```text
PantheonRevit:<task_id>:<action_id>:<short_effect>
```

The add-in should journal:

```text
binding and Revit versions
document reference
transaction name
Task Contract and approval references
started_at and finished_at
created, modified and deleted ElementIds where available
warnings and failures
commit, rollback or partial-result observation
manual reversal note when rollback is unavailable
```

The add-in must not silently save or synchronize after a successful transaction.

## Blockers and partial continuation

Missing families, invalid hosts, pinned/grouped elements, linked targets, worksharing locks, stale selections, phase conflicts and design-option conflicts are typed technical observations.

They do not create a second task-state machine. The existing Work Issue, run events, Result Candidate and Capability Gap surfaces carry the state. Hermes may continue only independent admitted actions; it may not create a hidden queue, silently change the method or resume a stale write without a new preflight.

## Source, Claim and Evidence posture

A Revit observation preserves:

```text
source document identity
binding and Revit version
view, selection and element references
observation time
method and parameters
technical warnings
```

```text
observed != validated
absence_of_warning != safe
transaction_success != professional_validation
action_log != Evidence
screenshot != proof
Result Candidate != accepted result
Evidence Pack Candidate != Evidence admitted
```

A consequential model proposal should remain a ChangeCandidate until reviewed and applied through its owning authority. The adapter must not mutate unrelated Project records directly.

## Sandbox and production

A disposable local copy may use a reviewed sandbox policy for learning. Sandbox freedom does not alter canonical authority, authorize production use or make destructive effects acceptable.

Real, workshared, linked, contractual or client models require stricter binding policy. Save, sync, purge, linked-model writes, arbitrary generated code and silent background mutation remain refused by the current boundary.

## Adoption gates

Before any binding is adopted or activated, verify together:

```text
implementation repository and owner
exact Revit and .NET compatibility
signed or otherwise reviewed package identity
installer and rollback procedure
no silent startup mutation
no implicit save, sync, purge or publication
visible stop/disable control
Task Contract and Context Pack correlation
fresh preflight and named transactions
changed-element trace
refusal of out-of-scope and stale operations
human gate before consequential effects
sandbox and production profiles separated
secrets and sensitive local paths redacted
one read-only acceptance and one rollback proof
```

## Supporting documents

The following documents are subordinate specializations, not parallel authorities:

```text
REVIT_CAPABILITY_EXPLORATION.md
  capability catalogue and exploratory scenarios

REVIT_LOCAL_ADAPTER_SANDBOX_POLICY.md
  optional disposable-copy sandbox posture

REVIT_LOCAL_ADAPTER_SANDBOX_ACTION_EXAMPLE.md
  first bounded action example

REVIT_LOCAL_ADAPTER_PROTOTYPE_PLAN.md
  product-version and implementation planning

REVIT_LOCAL_ADAPTER_DEVELOPER_DOSSIER.md
  detailed developer-oriented planning

revit-plugin/
  non-executable reference skeleton
```

Where a supporting document conflicts with this boundary or later canonical schemas and registries, this boundary and the canonical owner prevail.

## Final rule

```text
Pantheon governs.
Hermes may orchestrate an admitted task.
The Revit add-in executes locally.
Cockpit exposes the decision surface.
The human decides consequential effects.
Technical success remains technical evidence only.
```
'''
          (root / 'docs/governance/REVIT_LOCAL_ADAPTER.md').write_text(canonical, encoding='utf-8')

          supporting = {
              'docs/governance/REVIT_CAPABILITY_EXPLORATION.md': (
                  '# Revit Capability Exploration Dossier',
                  'Status: supporting planning dossier — documented non-implemented — subordinate to `REVIT_LOCAL_ADAPTER.md`.'
              ),
              'docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_POLICY.md': (
                  '# Revit Local Adapter — Sandbox Policy',
                  'Status: supporting sandbox policy — documented non-implemented — subordinate to `REVIT_LOCAL_ADAPTER.md`.'
              ),
              'docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_ACTION_EXAMPLE.md': (
                  '# Revit Local Adapter — Sandbox Action Example',
                  'Status: supporting action example — documented non-implemented — subordinate to `REVIT_LOCAL_ADAPTER.md`.'
              ),
              'docs/governance/REVIT_LOCAL_ADAPTER_PROTOTYPE_PLAN.md': (
                  '# Revit Local Adapter — Prototype Plan',
                  'Status: supporting implementation plan — documented non-implemented — subordinate to `REVIT_LOCAL_ADAPTER.md`.'
              ),
              'docs/governance/REVIT_LOCAL_ADAPTER_DEVELOPER_DOSSIER.md': (
                  '# Revit Local Adapter — Developer Dossier',
                  'Status: supporting developer dossier — documented non-implemented — subordinate to `REVIT_LOCAL_ADAPTER.md`.'
              ),
          }

          notice = (
              '\nCanonical placement: `REVIT_LOCAL_ADAPTER.md`. This file preserves a '
              'specialized catalogue, sandbox example or implementation plan only. It '
              'must not redefine authority, lifecycle, approval, Evidence or runtime ownership.\n'
          )

          for raw_path, (heading, status) in supporting.items():
              path = root / raw_path
              text = path.read_text(encoding='utf-8')
              lines = text.splitlines()
              if not lines:
                  raise SystemExit(f'empty document: {raw_path}')
              lines[0] = heading
              for index, line in enumerate(lines[:12]):
                  if line.startswith('Status:'):
                      lines[index] = status
                      break
              else:
                  lines.insert(1, '')
                  lines.insert(2, status)
              text = '\n'.join(lines) + ('\n' if text.endswith('\n') else '')
              if 'Canonical placement: `REVIT_LOCAL_ADAPTER.md`.' not in text:
                  marker = status + '\n'
                  text = text.replace(marker, marker + notice, 1)
              text = text.replace('blocked_v0', 'blocked_initial_sandbox')
              text = re.sub(r'\bV0\b', 'initial sandbox', text)
              text = text.replace('Pantheon Revit Gate', 'Revit local adapter')
              path.write_text(text, encoding='utf-8')

          readme = '''# Revit Local Adapter Reference Skeleton

Status: non-executable reference skeleton — documented non-implemented.

Canonical boundary: `../docs/governance/REVIT_LOCAL_ADAPTER.md`.

This directory is a reference layout for a future external Revit add-in. It is not the Pantheon governance kernel and it does not currently contain a compiling add-in, `.addin` manifest, Revit command, relay, MCP server, installer or runnable integration.

```text
Pantheon Next governs contracts and status.
Hermes may orchestrate an admitted task.
The external Revit add-in executes inside Revit.
Cockpit exposes preflight and review.
The human decides consequential effects.
```

## Code-hosting boundary

Production add-in code should live in a dedicated implementation repository. This skeleton may illustrate contracts and fixtures only. Its presence does not adopt a binding or authorize this repository to become a runtime, installer or plugin manager.

## Current reference content

```text
revit-plugin/
  README.md
  STATUS.md
  docs/
    ARCHITECTURE.md
    EXECUTION_MODEL.md
    KNOWLEDGE_SOURCE_POLICY.md
    PYTHON_AND_GENERATED_SNIPPETS.md
    CONTEXT_PACK_CONTRACT.md
    ACTION_LOG_CONTRACT.md
  src/.gitkeep
  tools/python/README.md
  samples/
    context-pack.example.json
    action-log.example.jsonl
```

## Implementation metadata

The currently reviewed prototype target may be Autodesk Revit 2027 with C# and .NET 10. Product and runtime versions are binding metadata; they are not part of the architecture identity.

## First admissible proof

```text
read the active document and view
read the explicit selection
produce a bounded Context Pack candidate
return a local technical trace
refuse stale or out-of-scope work
```

Any writable proof requires a named Revit transaction, exact Task Contract correlation, human authorization, changed-element journaling and a demonstrated rollback or manual-reversal posture.

```text
compiled != installed
loaded != activated
transaction_success != accepted_result
runtime_success != Evidence
```
'''
          (root / 'revit-plugin/README.md').write_text(readme, encoding='utf-8')

          architecture = '''# Revit Local Adapter Implementation Shape

Status: supporting implementation note — documented non-implemented — subordinate to `docs/governance/REVIT_LOCAL_ADAPTER.md`.

The canonical responsibility boundary is defined by `../../docs/governance/REVIT_LOCAL_ADAPTER.md`. This note records only the intended external implementation shape.

```text
Pantheon Next -> governance, schemas, status and human gates
pantheon-mvp  -> candidate APIs and Cockpit projections when implemented
Hermes        -> admitted external orchestration
Revit add-in  -> Revit API execution inside Revit context
Human         -> consequential decision
```

## First proof target

```text
read active document, view and explicit selection
materialize one bounded Context Pack candidate
perform no hidden global search
return a technical trace
refuse an entity outside the admitted scope
```

A later write slice must add fresh preflight, named transactions, changed-element journaling and rollback proof before it can be reviewed.

This file implements nothing.
'''
          (root / 'revit-plugin/docs/ARCHITECTURE.md').write_text(architecture, encoding='utf-8')

          def replace_row(path: Path, key: str, replacement: str) -> None:
              text = path.read_text(encoding='utf-8')
              lines = text.splitlines()
              for index, line in enumerate(lines):
                  if line.startswith(f'| `{key}` |'):
                      lines[index] = replacement
                      path.write_text('\n'.join(lines) + '\n', encoding='utf-8')
                      return
              raise SystemExit(f'authority row not found: {key}')

          authority = root / 'docs/governance/authority/ARCHITECTURE_AUTHORITY_INDEX.md'
          authority_text = authority.read_text(encoding='utf-8')
          canonical_row = '| `docs/governance/REVIT_LOCAL_ADAPTER.md` | candidate support doctrine / canonical adapter boundary | documented non-implemented | Single authority for the Revit local-adapter placement. Reuses Capability Slot, Task Contract, Context Pack, ChangeCandidate, Work Issue, Trace and Evidence Pack Candidate; assigns Revit API execution to an external add-in and preserves human gates. No runtime or add-in is implemented here. |'
          exploration_marker = '| `docs/governance/REVIT_CAPABILITY_EXPLORATION.md` |'
          if canonical_row not in authority_text:
              if exploration_marker not in authority_text:
                  raise SystemExit('Revit exploration authority row missing')
              authority_text = authority_text.replace(exploration_marker, canonical_row + '\n' + exploration_marker, 1)
              authority.write_text(authority_text, encoding='utf-8')

          replace_row(
              authority,
              'docs/governance/REVIT_CAPABILITY_EXPLORATION.md',
              '| `docs/governance/REVIT_CAPABILITY_EXPLORATION.md` | supporting planning dossier | documented non-implemented | Subordinate capability catalogue and exploratory scenarios. It cannot redefine adapter authority, lifecycle, approval, Evidence or runtime ownership established by `REVIT_LOCAL_ADAPTER.md`. |'
          )
          replace_row(
              authority,
              'docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_POLICY.md',
              '| `docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_POLICY.md` | supporting sandbox policy | documented non-implemented | Optional disposable-copy sandbox posture subordinate to `REVIT_LOCAL_ADAPTER.md`; it does not authorize production, save, sync, purge, linked-model writes, arbitrary code or Evidence admission. |'
          )
          replace_row(
              authority,
              'docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_ACTION_EXAMPLE.md',
              '| `docs/governance/REVIT_LOCAL_ADAPTER_SANDBOX_ACTION_EXAMPLE.md` | supporting action example | documented non-implemented | One bounded context -> candidate -> light-write -> trace example. It introduces no new lifecycle or approval model. |'
          )
          replace_row(
              authority,
              'docs/governance/REVIT_LOCAL_ADAPTER_PROTOTYPE_PLAN.md',
              '| `docs/governance/REVIT_LOCAL_ADAPTER_PROTOTYPE_PLAN.md` | supporting implementation plan | documented non-implemented | Product-version and implementation planning only. Revit and .NET versions are binding metadata, not architecture identities. |'
          )
          replace_row(
              authority,
              'docs/governance/REVIT_LOCAL_ADAPTER_DEVELOPER_DOSSIER.md',
              '| `docs/governance/REVIT_LOCAL_ADAPTER_DEVELOPER_DOSSIER.md` | supporting developer dossier | documented non-implemented | Detailed implementation planning subordinate to the canonical boundary. It does not establish a runtime, add-in, schema or approval authority. |'
          )

          what_runs = root / 'docs/governance/WHAT_RUNS.md'
          what_text = what_runs.read_text(encoding='utf-8')
          what_lines = what_text.splitlines()
          replacements = {
              '| Revit Gate |': '| Revit local adapter boundary | candidate support doctrine / documented non-implemented | `docs/governance/REVIT_LOCAL_ADAPTER.md` is the canonical placement for a future external add-in; specialized catalogues and sandbox notes are subordinate. | No Revit runtime, installation, binding activation or task authorization exists in Pantheon. |',
              '| `revit-plugin/` |': '| `revit-plugin/` | non-executable reference skeleton / documented non-implemented | Reference contracts and fixtures for a future external implementation repository. | Skeleton presence is not a compiling add-in, selected binding, installation, activation or code-hosting authority. |',
          }
          seen = set()
          for index, line in enumerate(what_lines):
              for prefix, replacement in replacements.items():
                  if line.startswith(prefix):
                      what_lines[index] = replacement
                      seen.add(prefix)
          if seen != set(replacements):
              raise SystemExit(f'missing WHAT_RUNS rows: {set(replacements) - seen}')
          what_runs.write_text('\n'.join(what_lines) + '\n', encoding='utf-8')

          log = '''# Revit local adapter authority convergence

Date: 2026-08-04
Status: validation-only intervention trace.

## Objective

Resolve issue #501 by rebuilding the useful historical Revit boundary from current `main`, without merging the divergent generation-named branch or creating a parallel execution model.

## Observed state

- the historical branch was four commits ahead and eighty-seven commits behind current `main`;
- current `main` already contained capability exploration, sandbox, action-example, prototype and developer documents;
- those documents overlapped but did not identify one canonical adapter boundary;
- `revit-plugin/` remained a non-compiling skeleton inside the governance repository.

## Convergence

- add `REVIT_LOCAL_ADAPTER.md` as the single responsibility boundary;
- map numbered historical warning levels to existing requested-effect classes instead of retaining a second approval vocabulary;
- carry blockers through existing Work Issue, run, Result Candidate and Capability Gap surfaces;
- keep Revit API execution in an external add-in;
- keep Task Contract, Context Pack, ChangeCandidate, Trace, Evidence and human gates in their current owners;
- rename active Revit documents according to responsibility;
- retain specialized material as explicitly subordinate support;
- clarify that `revit-plugin/` is a reference skeleton, not a runtime-code precedent.

```text
preflight_passed != effect_authorized
transaction_success != accepted_result
runtime_success != Evidence
```

No schema, route, runtime, installer, scheduler, queue, provider router, plugin manager, memory engine or automatic approval is added.
'''
          log_path = root / 'ai_logs/2026/Q3/2026-08-04-revit-local-adapter-convergence.md'
          log_path.parent.mkdir(parents=True, exist_ok=True)
          log_path.write_text(log, encoding='utf-8')

          active_files = [
              root / 'docs/governance/REVIT_LOCAL_ADAPTER.md',
              *(root / path for path in supporting),
              root / 'revit-plugin/README.md',
              root / 'revit-plugin/docs/ARCHITECTURE.md',
          ]
          for path in active_files:
              text = path.read_text(encoding='utf-8')
              if re.search(r'\bV0\b|\bv0\b', text):
                  raise SystemExit(f'generation label remains in active Revit document: {path}')

          for old in renames:
              old_name = Path(old).name
              for path in root.rglob('*'):
                  if not path.is_file() or 'ai_logs' in path.parts or '.git' in path.parts:
                      continue
                  if path.suffix not in text_extensions:
                      continue
                  try:
                      text = path.read_text(encoding='utf-8')
                  except UnicodeDecodeError:
                      continue
                  if old in text or old_name in text:
                      raise SystemExit(f'active reference to retired path {old_name}: {path}')
          PY

          git diff --check
          git status --short

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/governance revit-plugin ai_logs/2026/Q3/2026-08-04-revit-local-adapter-convergence.md
          git commit -m "docs(revit): consolidate local adapter boundary"
          git push origin HEAD:${{ github.head_ref }}
